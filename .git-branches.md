# Estrutura de Branches

Este projeto utiliza uma estratégia de branches para separar desenvolvimento e produção.

## Branches

### 🌿 `main`
Branch principal do repositório. Contém o código mais estável.

### 🚀 `prod` (Produção)
Branch de produção. Contém o código que está rodando em produção no VPS.
- **URL da API**: `https://api.luanova.cloud`
- **Status**: Pronto para deploy em produção
- **Configuração**: Todas as referências apontam para o VPS

### 🔧 `dev` (Desenvolvimento)
Branch de desenvolvimento. Para trabalho diário e novas features.
- **URL da API**: Detecta automaticamente (localhost em dev, VPS em produção)
- **Status**: Em desenvolvimento ativo
- **Configuração**: Flexível para desenvolvimento local

## Como usar

### Para trabalhar em desenvolvimento:
```bash
git checkout dev
# Faça suas alterações
git add .
git commit -m "Sua mensagem"
git push origin dev
```

### Para fazer deploy em produção:
```bash
# 1. Certifique-se que está na branch dev
git checkout dev

# 2. Faça merge das alterações para prod
git checkout prod
git merge dev

# 3. Revise as alterações e faça push
git push origin prod

# 4. Volte para dev para continuar trabalhando
git checkout dev
```

### Para criar uma nova feature:
```bash
git checkout dev
git checkout -b feature/nome-da-feature
# Trabalhe na feature
git push origin feature/nome-da-feature
# Depois faça merge em dev
```

## Configuração de Ambiente

### Produção (`prod`)
- Todas as URLs apontam para `https://api.luanova.cloud`
- Variáveis de ambiente devem estar configuradas no VPS

### Desenvolvimento (`dev`)
- Detecta automaticamente o ambiente
- Em `localhost`: usa `http://localhost:3001`
- Em produção: usa `https://api.luanova.cloud`

