# Branches do Projeto

Este projeto utiliza uma estratégia de branches para separar desenvolvimento e produção.

## Branches Disponíveis

### 🌿 `main`
Branch principal do repositório. Contém o código mais recente e estável.

### 🔧 `dev`
Branch de desenvolvimento. Configurado para trabalhar com:
- API local: `http://localhost:3001/api`
- Fallback para desenvolvimento local
- Detecção automática de ambiente

**Uso:** Para desenvolvimento local e testes.

### 🚀 `prod`
Branch de produção. Configurado para:
- API de produção: `https://api.luanova.cloud/api`
- Sempre usa a URL do VPS
- Otimizado para deploy em produção

**Uso:** Para deploy em produção no VPS.

## Como Usar

### Desenvolvimento Local
```bash
git checkout dev
npm run dev
```

### Deploy em Produção
```bash
git checkout prod
npm run build
npm run start
```

## Fluxo de Trabalho Recomendado

1. **Desenvolvimento:** Trabalhe no branch `dev`
2. **Testes:** Teste localmente no `dev`
3. **Merge para main:** Quando estiver pronto, faça merge do `dev` para `main`
4. **Deploy:** Faça merge do `main` para `prod` e faça o deploy

## Diferenças entre Branches

### `dev`
- Fallback: `http://localhost:3001/api`
- Detecção automática de ambiente
- Ideal para desenvolvimento local

### `prod`
- Fallback: `https://api.luanova.cloud/api`
- Sempre usa URL de produção
- Otimizado para VPS

