# ============================================
# Configuração do Ambiente - EstoqueLua Backend
# ============================================
# 
# INSTRUÇÕES:
# 1. Copie este arquivo para .env: cp .env.example .env
# 2. Preencha todas as variáveis obrigatórias
# 3. Ajuste as variáveis opcionais conforme necessário
# 4. NUNCA commite o arquivo .env no Git!
#
# ============================================

# ============================================
# OBRIGATÓRIAS
# ============================================

# Ambiente de execução
# Valores: development | production | test
# Padrão: development
NODE_ENV=production

# Porta do servidor
# Padrão: 3001
PORT=3001

# URL do banco de dados
# SQLite (desenvolvimento):
# DATABASE_URL="file:./prisma/dev.db"
#
# MySQL (produção):
# DATABASE_URL="mysql://usuario:senha@host:porta/database"
#
# PostgreSQL (produção alternativa):
# DATABASE_URL="postgresql://usuario:senha@host:porta/database"
#
# OBRIGATÓRIO EM PRODUÇÃO!
DATABASE_URL=mysql://user:password@localhost:3306/estoquelua

# Secret para JWT Access Token
# MÍNIMO 32 CARACTERES!
# Gere uma string aleatória segura com:
#   openssl rand -base64 32
#   ou
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=your-super-secret-jwt-key-change-in-production-min-32-chars-here

# Secret para JWT Refresh Token
# MÍNIMO 32 CARACTERES!
# Gere uma string aleatória segura diferente do JWT_SECRET
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-in-production-min-32-chars-here

# ============================================
# OPCIONAIS
# ============================================

# CORS - Origens permitidas
# Separe múltiplas origens por vírgula
# Em produção, apenas origens configuradas aqui serão permitidas
# (luanova.cloud já está hardcoded como permitida)
# Exemplo: https://app.example.com,https://www.example.com
CORS_ORIGINS=https://luanova.cloud,https://www.luanova.cloud

# Rate Limiting - Janela de tempo (em milissegundos)
# Padrão: 900000 (15 minutos)
# Exemplo: 600000 = 10 minutos, 1800000 = 30 minutos
RATE_LIMIT_WINDOW_MS=900000

# Rate Limiting - Número máximo de requisições por IP
# Padrão: 100 (em produção), 1000 (em desenvolvimento)
# Ajuste conforme necessário para sua aplicação
RATE_LIMIT_MAX_REQUESTS=100

# ============================================
# NOTAS IMPORTANTES
# ============================================
#
# 1. SEGURANÇA:
#    - NUNCA compartilhe seus secrets JWT
#    - Use secrets diferentes para JWT_SECRET e JWT_REFRESH_SECRET
#    - Em produção, use secrets com pelo menos 32 caracteres
#    - Gere secrets aleatórios e seguros
#
# 2. BANCO DE DADOS:
#    - SQLite é apenas para desenvolvimento local
#    - Em produção, use MySQL ou PostgreSQL
#    - DATABASE_URL é obrigatório em produção
#
# 3. CORS:
#    - Em desenvolvimento, todas as origens são permitidas
#    - Em produção, apenas origens configuradas são permitidas
#    - luanova.cloud já está hardcoded como permitida
#
# 4. RATE LIMITING:
#    - Ajuste conforme o uso esperado da API
#    - Valores muito baixos podem bloquear usuários legítimos
#    - Valores muito altos podem não proteger contra DDoS
#
# ============================================
